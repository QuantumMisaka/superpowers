# 同步上游 obra/superpowers v6.2.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or executing-plans. Steps use checkbox syntax.

**Goal:** 将上游 obra/superpowers main（v6.2.0，20 commits）整体同步进 QuantumMisaka 自用 fork，保持本地多模型（deepseek/qwen/GPT）适配与多平台支持不回归。

**Spec:** 用户 2026-08-03 确认（Q1A–Q6A）：整体同步；跟随上游 SDD plan-scoped 机制；routing 以本地 dual-provider 为基底；保留本地专属测试；全量回归；push origin。

**Architecture:** 无共同祖先的两条历史线，在隔离 worktree 分支 `sync/upstream-v6.2.0` 上 `git merge upstream/main --allow-unrelated-histories`，49 个 add/add 冲突按 5 个主题逐文件协调；8 个上游新增文件直接采用；本地独有文件自动保留。每主题独立提交 + 对应测试；最终全量回归。

**Tech Stack:** git merge（unrelated histories）、bash（上游 tests/）、本地 ~/.codex 环境。

## Global Constraints

- 合并演练已确认冲突面：49 冲突 + 8 新增；本计划不修改 main，全部工作在 worktree 分支完成，最终由用户确认后合回 main 并 push origin。
- Q2A：SDD 机制以上游 v6.2.0（plan-scoped workspace、resume-based fix loop）为基底；本地脚本约定适配。
- Q3A：routing 参考（codex-tools.md）以本地 Capability-Aware Routing / dual-provider 抽象为基底，吸收上游通用更新。
- Q4A：本地专属测试（codex-subagent-routing / adaptive-workflow-routing / positive-evidence-language）保留并适配。
- 上游自带测试均为 bash 脚本（tests/ 下），本机 Linux 全量跑通为合入门禁。
- 每主题提交信息：`sync(upstream-v6.2.0): <主题>`。

---

### Task 0: 合并现场与基底确认

- [x] 在 worktree 执行 `git merge upstream/main --allow-unrelated-histories --no-commit`（保留冲突现场用于本计划后续主题）
- [x] 记录 49 冲突 + 8 新增文件清单（已确认）
- [x] 基线：跑上游 `tests/skill-content/run-tests.sh` 冒烟（预期当前本地 main 状态有失败属正常，记录基线）

### Task 1: SDD 机制跟随上游（7 冲突 + 1 新增）

**Files:** skills/subagent-driven-development/SKILL.md、implementer-prompt.md、task-reviewer-prompt.md、scripts/review-package、scripts/sdd-workspace、scripts/task-brief；新增 re-review-prompt.md
**策略:** 上游 v6.2.0 为基底（plan-scoped workspace：`/.superpowers/sdd/<plan>/`；resume-based fix loop；五轮 breaker；rationalization table）。task-brief/review-package 本地与上游仅差 1-2 行，以上游为准并核对本地报告路径约定；implementer/task-reviewer prompt 以上游为主，若上游删除了本地"report 文件追加 fix 报告"约定则保留本地段落。
**验证:** tests/claude-code/test-sdd-workspace.sh、test-subagent-driven-development.sh（上游新版）

### Task 2: Routing 参考（2 冲突 + 1 新增）

**Files:** skills/using-superpowers/references/codex-tools.md、antigravity-tools.md；新增 gemini-tools.md
**策略:** codex-tools.md 以本地 176 行为基底（Capability-Aware Routing 抽象角色表、dual-provider 段落、`agent_type`/`fork_turns` 处理），吸收上游 39 行通用更新（Environment Detection、Codex App Finishing、reviewer close 时机、detached HEAD 处理）。antigravity-tools.md 差异小，上游为基底保留本地特有内容。gemini-tools.md 直接采用上游新增。
**验证:** tests/skill-content/test-codex-subagent-routing-contract.sh（本地专属，适配新语义）

### Task 3: 通用 skills 逐文件协调（14 文件）

**Files:** using-superpowers/SKILL.md、systematic-debugging/SKILL.md + find-polluter.sh、test-driven-development/SKILL.md + writing-good-tests.md、verification-before-completion/SKILL.md、writing-plans/SKILL.md、writing-skills/SKILL.md、brainstorming/SKILL.md + spec-document-reviewer-prompt.md + visual-companion.md、dispatching-parallel-agents/SKILL.md、executing-plans/SKILL.md、finishing-a-development-branch/SKILL.md、receiving-code-review/SKILL.md、requesting-code-review/SKILL.md + code-reviewer.md
**策略:** 逐文件 diff（本地 vs 上游）判断：本地定制少（<10 行差异或仅为本地段落追加）→ 上游为基底 + 重放本地段落；本地深度定制（如 using-superpowers 的 L1/L2/L3 中文语义、TDD contract-subdomain）→ 本地为基底 + 吸收上游修复。find-polluter.sh 必须吸收上游 `./` 前缀修复（本地缺失）。
**验证:** tests/systematic-debugging/test-find-polluter.sh（上游新增）+ skill-content 相关

### Task 4: 平台/打包/文档（12 文件）

**Files:** .claude-plugin/*、.codex-plugin/plugin.json、.cursor-plugin/plugin.json、.kimi-plugin/plugin.json、CLAUDE.md、README.md、RELEASE-NOTES.md、docs/porting-to-a-new-harness.md、docs/testing.md、docs/windows/polyglot-hooks.md、gemini-extension.json、hooks/hooks.json、package.json、scripts/package-codex-plugin.sh
**策略:** 本地多平台支持（kimi/opencode/pi/cursor/gemini）为基底，吸收上游通用更新（README hiring 移除、RELEASE-NOTES 补 v6.2.0、package-codex-plugin.sh 可移植性修复、hooks SessionStart 更新——Windows 部分保留但标注本地 Linux 不适用）。
**验证:** tests/codex/test-package-codex-plugin.sh、tests/hooks/test-session-start.sh、tests/pi/test-pi-extension.mjs、tests/antigravity/test-antigravity-tools.sh

### Task 5: 测试适配（9 冲突 + 1 新增）

**Files:** tests/antigravity/test-antigravity-tools.sh、tests/claude-code/{run-skill-tests.sh,test-helpers.sh,test-sdd-workspace.sh,test-subagent-driven-development.sh}、tests/codex/test-package-codex-plugin.sh、tests/hooks/test-session-start.sh、tests/pi/test-pi-extension.mjs、新增 tests/systematic-debugging/test-find-polluter.sh
**策略:** 上游新版为基底；本地专属断言重新应用；test-sdd-workspace.sh 必须匹配 Task 1 新机制。本地专属测试（codex-subagent-routing/adaptive-workflow/positive-evidence）检查与上游 SKILL 新语义兼容性并适配。
**验证:** 全量 tests/ 跑通

### Task 6: 回归、记录与发布

- [x] 全量上游 tests（tests/ 下所有 run-tests 脚本）+ 本地专属测试
- [x] RELEASE-NOTES.md 记录本次同步（v6.2.0 fork sync）
- [x] 最终 review（subagent-driven-development final reviewer）
- [x] 用户确认后：merge 回 main、push origin（QuantumMisaka/superpowers）
